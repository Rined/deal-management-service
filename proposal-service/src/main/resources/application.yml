spring:
  application:
    name: "proposal"
  data:
    mongodb:
      port: 0
      database: proposal-service
#      uri: mongodb://localhost
#      port: 27017
#      database: proposal-service

logging:
  level:
    root: info
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

server:
  port: 8082
