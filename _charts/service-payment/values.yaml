replicaCount: 1
updateType: RollingUpdate
host: arch.homework
fullnameOverride: payment-service

image: rined/dms-payment:v1

service:
  type: NodePort
  port: 9000

postgresql:
  fullnameOverride: pg-db
  postgresqlUsername: rined
  postgresqlPassword: password
  postgresqlDatabase: otus_payment_app
  service:
    port: "5432"
